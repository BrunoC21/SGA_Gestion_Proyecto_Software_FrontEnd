import { SliceProductsPipe } from './slice-products.pipe';

describe('SliceProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
